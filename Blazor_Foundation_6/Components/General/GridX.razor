
<div class="@Class">
    @ChildContent
</div>

@code {
    [Parameter]
    public RenderFragment ChildContent { get; set; }

    [Parameter]
    public string Class { get; set; }

    [Parameter]
    public bool Margin { get; set; }

    [Parameter]
    public bool Padding { get; set; }


    protected override async Task OnInitializedAsync()
    {
        if (Class != null)
        {
            Class = "grid-x " + Class;
        }
        else
        {
            Class = "grid-x";
        }

        if (Margin) { Class += $" grid-margin-x"; }
        if (Padding) { Class += $" grid-padding-x"; }
    }
}

